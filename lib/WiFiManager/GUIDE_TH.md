# คู่มือการใช้งาน WiFiManager สำหรับมือใหม่ (ฉบับละเอียด) 🚀

ยินดีต้อนรับสู่ไลบรารี **esp32-wifiManager**! คู่มือนี้จะรวบรวมฟังก์ชันการใช้งานทั้งหมดที่มีในไลบรารี เพื่อให้คุณดึงประสิทธิภาพของ ESP32 ออกมาได้สูงสุด

---

### 1. การติดตั้ง (Installation)
เปิดไฟล์ `platformio.ini` แล้วเพิ่มใน `lib_deps`:
```ini
lib_deps =
    https://github.com/noobtohero/esp32-wifiManager.git
```

---

### 2. ฟังก์ชันพื้นฐาน (Core API)

#### `begin(apName, apPassword)`
ใช้เริ่มต้นทำงานและกำหนดชื่อ WiFi สำหรับตั้งค่า
*   **apName:** ชื่อ WiFi (Hotspot) ที่บอร์ดจะปล่อย (ค่าเริ่มต้น: ESP32-Smart-Portal)
*   **apPassword:** รหัสผ่านสำหรับ Hotspot (ถ้าไม่ใส่จะเป็นแบบเปิด)

```cpp
wifiManager.begin("My-ESP32-Setup", "12345678");
```

#### `resetSettings()`
ใช้ล้างข้อมูล WiFi ที่เคยบันทึกไว้ทั้งหมด (เครื่องจะรีสตาร์ทด้วย)
```cpp
wifiManager.resetSettings();
```

#### `isConnected()`
เช็คว่าขณะนี้เชื่อมต่อ WiFi สำเร็จหรือไม่ (คืนค่า true/false)
```cpp
if(wifiManager.isConnected()) { ... }
```

#### `getSSID()`
ดึงชื่อ WiFi (SSID) ที่กำลังเชื่อมต่ออยู่ปัจจุบัน
```cpp
Serial.println(wifiManager.getSSID());
```

---

### 3. การจัดการระบบเวลา (Time Management) 🕒
เมื่อบอร์ดเชื่อมต่อเน็ตได้ ระบบจะซิงค์เวลาจาก NTP Server ให้โดยอัตโนมัติ

*   **`now()`**: คืนค่า วันที่และเวลา (เช่น 2026-01-10 12:00:00)
*   **`date()`**: คืนค่า วันที่ (เช่น 2026-01-10)
*   **`time()`**: คืนค่า เวลา (เช่น 12:00:00)
*   **`getTimestamp()`**: คืนค่า Unix Timestamp (เลขวินาที)
*   **`isTimeSynced()`**: เช็คว่าเวลาซิงค์สำเร็จหรือยัง

```cpp
Serial.println("Current Time: " + wifiManager.now());
```

---

### 4. การจัดการไฟ LED (Status LED) 💡

*   **`setStatusLED(pin, activeLow)`**: กำหนดขาไฟ LED (Active Low คือไฟติดเมื่อขาเป็น LOW)
*   **`enableStatusLED(bool)`**: เปิดหรือปิดการทำงานของไฟสถานะ
*   **`setLEDActiveTime(ms)`**: กำหนดความเร็วในการกระพริบ (ค่าเริ่มต้น 150ms)

```cpp
wifiManager
  .setStatusLED(2, false) // ใช้ขา 2, ไฟติดเมื่อเป็น HIGH
  .setLEDActiveTime(100)  // กระพริบไวขึ้น
  .begin();
```

---

### 5. โหมดขั้นสูง (Middleware Mode) 🧠
หากคุณมีระบบ Dashboard หรือ OTA ของตัวเอง และต้องการใช้ Port 80 ร่วมกัน

*   **`useServer(WebServer *server)`**: บอกให้ WiFiManager ไปใช้ Server ตัวเดียวกับคุณ
*   **`getServer()`**: ดึง Pointer ของ WebServer ที่ WiFiManager ใช้อยู่มาใช้งาน

```cpp
WebServer myServer(80);

void setup() {
  myServer.on("/data", [](){ myServer.send(200, "text/plain", "My Data"); });
  
  wifiManager.useServer(&myServer).begin();
  myServer.begin();
}
```

---

### 6. ระบบ Callback (Event Handling) ⚓
คุณสามารถสั่งให้บอร์ดทำงานบางอย่างเมื่อเกิดเหตุการณ์ต่างๆ ได้

*   **`onConnected(cb)`**: ทำงานเมื่อเชื่อมต่อ WiFi สำเร็จ
*   **`onDisconnected(cb)`**: ทำงานเมื่อ WiFi หลุด
*   **`onPortal(cb)`**: ทำงานเมื่อบอร์ดเริ่มปล่อย Hotspot สำหรับตั้งค่า
*   **`onTimeout(cb)`**: ทำงานเมื่อเครื่องอยู่ในโหมดตั้งค่านานเกินไป (Timeout)
*   **`onStatusChange(cb)`**: ทำงานทุกครั้งที่มีการเปลี่ยนสถานะ (Connected, Disconnected, etc.)
*   **`onSleepChange(cb)`**: ทำงานเมื่อเข้าหรือออกจากโหมดประหยัดพลังงาน

```cpp
wifiManager.onConnected([](){
    Serial.println("Internet is Ready!");
});
```

---

### 7. การประหยัดพลังงาน (Power Management) 🔋

*   **`setSleep(bool)`**: เปิดหรือปิดโหมดประหยัดพลังงาน (Modem Sleep) โดยปกติจะเปิดให้อัตโนมัติเมื่อต่อ WiFi ได้
*   **`isSleepEnabled()`**: เช็คว่าปัจจุบันโหมดประหยัดพลังงานเปิดอยู่หรือไม่
*   **`wakeUp()`**: สั่งให้เครื่องตื่นจากการหลับ (ถ้า AP ปิดไปแล้วเพราะ Timeout ฟังก์ชันนี้จะสั่งให้ AP และ Portal กลับมาทำงานใหม่ทันที) เหมาะสำหรับใช้คู่กับปุ่มกดบนบอร์ด

```cpp
// ตัวอย่าง: กดปุ่มที่บอร์ดแล้วให้เปิดหน้าตั้งค่า WiFi ขึ้นมาใหม่
if(digitalRead(BUTTON_PIN) == LOW) {
    wifiManager.wakeUp();
}
```

---

### 💡 สรุปการดูสถานะจากไฟ LED
*   **กระพริบเร็วมาก:** อยู่ในโหมดตั้งค่า (Portal Mode)
*   **กระพริบกลางๆ:** กำลังพยายามต่อ WiFi (Connecting)
*   **กระพริบช้า (Fade):** ต่อสำเร็จและทำงานปกติ
*   **กระพริบช้ามาก (5 วินาที):** อยู่ในโหมดประหยัดพลังงาน (Modem Sleep)

---
จัดทำโดยทีมพัฒนาเพื่ออำนวยความสะดวกให้ชาว ESP32 ทุกคนครับ! 😊
